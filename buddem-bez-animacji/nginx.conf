server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  gzip on;
  gzip_comp_level 6;
  gzip_types text/plain text/css application/javascript application/json image/svg+xml;

  # Strona jest serwowana spod podscieżki /buddem-bez-animacji/ (patrz base w vite.config.js).
  # Coolify/Traefik może przekazać żądanie z prefiksem LUB bez niego —
  # obsługujemy oba warianty, żeby assety znalazły się niezależnie od konfiguracji.

  # Wariant BEZ obcinania prefiksu: proxy oddaje /buddem-bez-animacji/... — obcinamy tutaj.
  location = /buddem-bez-animacji { return 301 /buddem-bez-animacji/; }
  location /buddem-bez-animacji/ {
    rewrite ^/buddem-bez-animacji/(.*)$ /$1 break;
    try_files $uri $uri/ $uri.html =404;
  }

  # Zasoby z hashem w nazwie — cache na rok (wariant z obciętym prefiksem).
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # Wariant Z obcinaniem prefiksu: żądania trafiają już bez prefiksu.
  location / {
    try_files $uri $uri/ $uri.html =404;
  }
}
